build:
  performance:
    jobs:
      run-grakn:
        machine: grabl-ubuntu-2004
        type: background
        timeout: "18000"
        script: |
          echo 'hi'
    execution:
      - run-grakn